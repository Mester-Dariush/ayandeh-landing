<div align="center">
  <h1>Ayandeh | آینده</h1>
  <p>
    <strong>قالب حرفه‌ای لندینگ و صفحات پرداخت (مخصوص ربات‌های تلگرام)</strong>
  </p>

  <!-- Badges -->
  <p>
    <a href="https://react.dev/"><img src="https://img.shields.io/badge/React-19-blue?logo=react" alt="React"></a>
    <a href="https://tailwindcss.com/"><img src="https://img.shields.io/badge/Tailwind-3.4-cyan?logo=tailwindcss" alt="Tailwind"></a>
    <a href="https://vitejs.dev/"><img src="https://img.shields.io/badge/Vite-Fast-yellow?logo=vite" alt="Vite"></a>
  </p>
</div>

---

## 🇮🇷 داستان این پروژه چیه؟

آقا خلاصه داستان اینه: خیلی از ماها دولوپر بک‌اندیم یا داریم ربات تلگرام می‌نویسیم، ولی واسه گرفتن **اینماد** و **درگاه پرداخت** گیرِ یه سایت ظاهری هستیم. از اون طرف هم طراحی سایت و سروکله زدن با CSS واقعا حوصله میخواد! 😅

این پروژه یه قالب آماده و خوشگل (React + Tailwind) هست که این مشکل رو حل می‌کنه. فقط کافیه ران کنی و آدرسش رو بدی به پرداخت‌یار. 
یه قابلیت خفن دیگه هم داره واسه حل مشکل `Referer` که پایین‌تر توضیح دادم.

---

## 🚀 راهنمای استفاده از صفحات

این پروژه ۴ تا صفحه اصلی داره که کارتو راه میندازه:

### ۱. صفحه اصلی (Home)
- **آدرس:** `/`
- **کاربرد:** این ویترین سایت شماست. وقتی داری درخواست درگاه میدی، این صفحه رو نشون کارشناس پرداخت‌‎یار میدی تا ببینه سایتت بالا و "به زودی" هست. خیلی شیک و مجلسی طراحی شده.

### ۲. صفحه انتقال به درگاه (Redirect) - 🔥 خیلی مهم
- **آدرس:** `/payment/redirect?url=LINK`
- **کاربرد:** ببین، اگه لینک پرداخت رو مستقیم بدی به یوزر توی تلگرام، ممکنه درگاه خطا بده (چون هدر `Referer` خالیه).
- **راه حل:** لینک پرداخت رو میدی به این صفحه، این صفحه یه انیمیشن "اتصال امن" نشون میده و خودش کاربر رو می‌فرسته سمت بانک. اینجوری هدر ست میشه و درگاه گیر نمیده.
- **مثال:**
  `https://yoursite.com/payment/redirect?url=https://shaparak.ir/ipg/test`

### ۳. صفحه پرداخت موفق (Success)
- **آدرس:** `/payment/success?order_id=NUMBER`
- **کاربرد:** وقتی کاربر پول رو داد و برگشت توی بک‌اندِ تو، و تو تراکنش رو `Verify` کردی، کاربر رو ریدایرکت می‌کنی به اینجا.
- **مثال:**
  `https://yoursite.com/payment/success?order_id=140255`

### ۴. صفحه پرداخت ناموفق (Failed)
- **آدرس:** `/payment/failed?order_id=NUMBER`
- **کاربرد:** اگه پول نداد یا کنسل کرد، میفرستیش اینجا تا بدونه چی شده و برگرده به ربات.
- **مثال:**
  `https://yoursite.com/payment/failed?order_id=140255`

---

## ⚙️ شخصی‌سازی (بدون کدنویسی)

نیاز نیست بگردی تو کدها! کل متن‌ها، لینک ربات تلگرام و تنظیمات توی فایل `config.ts` هست.
این فایل رو باز کن، مقادیر رو عوض کن، تمام!

---

## 📦 نصب و اجرا

خیلی ساده‌ست، ترمینال رو باز کن:

1. **نصب پکیج‌ها:**
   ```bash
   npm install
   ```
2. **اجرای تستی (لوکال):**
   ```bash
   npm run dev
   ```
3. **خروجی نهایی (بیلد):**
   ```bash
   npm run build
   ```

---

## 🔥 دیپلوی (کجا آپلود کنیم؟)

### ️ سرور شخصی (VPS - Nginx/Apache)
1. دستور `npm run build` را اجرا کنید.
2. محتویات پوشه `dist` را در روت هاست خود آپلود کنید.
3. تنظیمات وب‌سرور را انجام دهید تا همه درخواست‌ها به `index.html` هدایت شوند:

**Nginx:**
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

**Apache:**
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### 🌐 هاست اشتراکی (cPanel / DirectAdmin)
1. دستور `npm run build` را اجرا کنید.
2. وارد **File Manager** هاست شوید و به پوشه `public_html` بروید.
3. محتویات پوشه `dist` (نه خود پوشه، بلکه فایل‌های داخلش) را آپلود کنید.
4. یک فایل به نام `.htaccess` در کنار `index.html` بسازید (اگر وجود دارد ویرایش کنید) و کد زیر را در آن قرار دهید:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```
*(این کد باعث می‌شود وقتی کاربر آدرس `/payment/success` را می‌زند، سرور به جای ارور ۴۰۴، فایل اصلی برنامه را اجرا کند)*

---

## ⚠️ نکته فنی مهم (مخصوص بک‌اند کارها)

رفقا دقت کنید! این پروژه فقط **فرانت‌اند (ظاهر)** کار هست.
فرایند وریفای کردن تراکنش (Verify Transaction) و چک کردن اینکه پول واقعا اومده یا نه، **باید سمت سرور خودتون (Bot Backend)** انجام بشه.
بعد از اینکه سمت خودتون اوکی بود، کاربر رو به صفحات `/payment/success` یا `/payment/failed` این قالب ریدایرکت کنید.

---
<div align="center">
  ساخته شده با ❤️ برای توسعه‌دهندگان ایرانی
</div>